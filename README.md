# mr-daebak-backend

## How to install

```
python3 -m venv .venv
. .venv/bin/activate
(venv) python -m pip install -r requirements.txt
```

## How to run

```
. .venv/bin/activate
(venv) python3 -m uvicorn main:app --reload
```
